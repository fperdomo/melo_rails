<% @articles.each do |art| %>
	<h1><%= link_to art.title, art %></h1>
	<div>
		<%= art.body.html_safe %> - <%= link_to "Eliminar", art, method: :delete, class:"red"%> - 
		<% if art.may_publish? %>
			<%= link_to "Publicar", "/articles/#{art.id}/publish", method: :put, class:"blue"%>
		<%end%>
	</div>
<%end%>