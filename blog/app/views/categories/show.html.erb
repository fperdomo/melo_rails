<p id="notice"><%= notice %></p>

<p>
  <h1 style="color:<%= @category.color %>"><%= @category.name %></h1> 
  <h2>Artículos</h2>   
</p>
<p>
	<% @category.articles.each do |art| %>
		<h3><%= link_to art.title, art %></h3>
		<div>
			<%= art.body %> - <%= link_to "Eliminar", art, method: :delete, class:"red"%>
		</div>
	<%end%>
</p>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
